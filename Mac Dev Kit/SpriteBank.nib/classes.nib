{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {hotSpotBase = id; hotSpotCentre = id; }; 
            CLASS = SpriteBank; 
            LANGUAGE = ObjC; 
            OUTLETS = {spriteIndexSlider = NSSlider; spriteView = SpriteOpenGLView; }; 
            SUPERCLASS = NSDocument; 
        }, 
        {
            CLASS = SpriteOpenGLView; 
            LANGUAGE = ObjC; 
            OUTLETS = {doc = id; }; 
            SUPERCLASS = NSOpenGLView; 
        }
    ); 
    IBVersion = 1; 
}