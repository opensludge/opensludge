{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {changeMode = id; loadBackdrop = id; setFloorColour = id; }; 
            CLASS = FloorDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                floorColourWell = NSColorWell; 
                floorView = FloorOpenGLView; 
                modeSelector = NSPopUpButton; 
            }; 
            SUPERCLASS = NSDocument; 
        }, 
        {
            CLASS = FloorOpenGLView; 
            LANGUAGE = ObjC; 
            OUTLETS = {doc = id; }; 
            SUPERCLASS = NSOpenGLView; 
        }
    ); 
    IBVersion = 1; 
}