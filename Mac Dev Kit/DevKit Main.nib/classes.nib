{
    IBClasses = (
        {
            ACTIONS = {
                closeProject = id; 
                compileMenu = id; 
                helpMenu = id; 
                newProject = id; 
                openProject = id; 
                prefKeepImagesClick = id; 
                prefVerboseClick = id; 
                prefWriteStringsClick = id; 
                prefsMenu = id; 
                saveProject = id; 
                saveProjectAs = id; 
                spriteBankNew = id; 
                spriteBankOpen = id; 
            }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                prefKeepImages = NSButton; 
                prefVerbose = NSButton; 
                prefWriteStrings = NSButton; 
                preferenceWindow = NSWindow; 
                projectFiles = NSTableView; 
                projectWindow = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = ProjectController; LANGUAGE = ObjC; SUPERCLASS = NSWindowController; }, 
        {
            ACTIONS = {
                help = id; 
                newGame = id; 
                openGame = id; 
                prefsMenu = id; 
                saveGame = id; 
                saveGameAs = id; 
            }; 
            CLASS = SDLMain; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}