{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelButton = id; okButton = id; }; 
            CLASS = StartController; 
            LANGUAGE = ObjC; 
            OUTLETS = {fullScreenCheck = NSButton; languageList = NSPopUpButton; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}